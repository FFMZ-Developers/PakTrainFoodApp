{
  "rules": {
    "Orders": {
      "$orderId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "OrderLocations": {
      "$orderId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "DeliveryRiders": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "Users": {
      "Passenger": {
        "$uid": {
          ".read": "auth != null && auth.uid == $uid",
          ".write": "auth != null && auth.uid == $uid"
        }
      }
    }
  }
}
